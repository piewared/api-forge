# Fly.io-specific overrides.
# These values are loaded by the CLI during `fly up` and are NOT used
# by Docker Compose or Kubernetes deployments.
PRODUCTION_DATABASE_URL=postgres://postgres@fly-db-70aa10da.internal:5432/postgres
