# Copy this file to 'infra/secrets/user-provided.env' and fill in real values.
# These secrets are NOT generated automatically and must be provided manually.
# Never commit the populated file – it is ignored via .gitignore.

# --- OIDC Provider Secrets ---
# Required for authenticating with external identity providers.
OIDC_GOOGLE_CLIENT_SECRET=replace-me
OIDC_MICROSOFT_CLIENT_SECRET=replace-me
OIDC_KEYCLOAK_CLIENT_SECRET=replace-me

# Example additional secrets you may want to centralize here.
# Uncomment if needed and referenced by your deployment scripts.
#FLY_IO_API_KEY=
